version: '2'
volumes:
  todolist-data:
services:
  todolist:
    build: ./todolist
    volumes:
      - 'todolist-data:/data'
    restart: always
    privileged: true
    network_mode: host
    labels:
      io.balena.features.supervisor-api: '1'
    ports:
      - 80:80
